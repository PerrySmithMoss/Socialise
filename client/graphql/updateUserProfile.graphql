mutation UpdateUserProfile(
  $userId: Int!
  $bio: String!
  $website: String!
  $location: String!
) {
  updateProfile(
    userId: $userId
    bio: $bio
    website: $website
    location: $location
  )
}
